interface:
  display_name: "LLM Playground"
  short_description: "Repo-specific patterns and workflows for LLM-Playground"
  default_prompt: "Use the LLM-Playground repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
